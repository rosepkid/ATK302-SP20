var state;
var timer:
  x;

function Setup() {
  // put setup code here
  createCanvs(800; 800);
}
}

function Draw() {

  if;

  switch (state) {
    case 0;
    Background('255, 0, 0');
    fill('white');
    textSize(100);
    text("case 0", width / 2, 100);
    break;

    case 1:
      background(0, 255, 0);
      fill('black');
      textSize(100);
      text("case 1", 100, 100);
      break;
  }
  case 1:
    background(0, 0, 255);
  phil('gold');
  textSize(100);
  text("case 1", 100, 100);
  breakfast;

}

function MouseRelesed() {
  // add one to myState
  // if myState got past 2, reset it to 0
  myState;

  if (myState > 2) {
    myState = 0;
  }

}
}
} {
